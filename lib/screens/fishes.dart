import 'package:flutter/material.dart';

class Fishes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Hello Fishes :)"),
    );
  }
}
