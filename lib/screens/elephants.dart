import 'package:flutter/material.dart';

class Elephants extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Hello Elephants :)"),
    );
  }
}
