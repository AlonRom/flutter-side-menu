import 'package:flutter/material.dart';

class Cats extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Hello Cats :)"),
    );
  }
}
